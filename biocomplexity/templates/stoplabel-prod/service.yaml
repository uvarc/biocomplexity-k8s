apiVersion: v1
kind: Service
metadata:
  name: stoplabel-prod-service
  namespace: biocomplexity
spec:
  selector:
    app: stoplabel-prod
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 10149
