apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
  name: dismolib-nginx-ingress
  namespace: biocomplexity
spec:
  rules:
    - host: dismolib.pods.uvarc.io
      http:
        paths:
          - backend:
              serviceName: dismolib-service
              servicePort: 8080
            path: /